#!/bin/bash
# switch1 · DISARM — benign "safe" payload: no HID, no network.
# Proves the LED / loot status pipeline end to end.
LED SETUP
sleep 1
mkdir -p /root/udisk/loot
echo "[$(date -u +%FT%TZ)] switch1 disarm demo ran on $(hostname)" >> /root/udisk/loot/run.log
LED B SLOW
sleep 2
LED OFF
LED FINISH
exit 0